import { colors } from "quasar"
const { getBrand } = colors

const accentColor = getBrand("accent")
const primaryColor = getBrand("primary")
const secondaryColor = getBrand("secondary")

export default [accentColor, primaryColor, secondaryColor]
